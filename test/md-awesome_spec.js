const chai = require('chai');
const expect = chai.expect;

describe('MdAwesome', () => {
  describe('#preview()', () => {
    it('should be ok', () => {
      expect(1).to.equal(1);
    });
  });
});
